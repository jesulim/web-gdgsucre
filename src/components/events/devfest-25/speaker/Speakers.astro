---
import speakers from "@/data/devfest-25/speakers.json"
import { SpeakerCard } from "./SpeakerCard"
---

<section id="speakers" class="w-full py-8 flex flex-col items-center gap-8">
  <div class="mx-auto my-2 max-w-7xl px-4 md:my-8">
    <h2 class="text-4xl lg:text-5xl font-bold mb-10 text-center">Speakers</h2>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-12">
      {speakers.map(data => <SpeakerCard {...data} />)}
    </div>
  </div>
</section>
